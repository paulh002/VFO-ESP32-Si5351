#ifndef _CREDENTIALS_H_			// Prevent double inclusion
#define _CREDENTIALS_H_

#define NUMBR_SSID  2
const char* _ssid[] = {"ssid1", "ssid2" };
const char* _password[] = {"Password1", "Password2"};

#endif